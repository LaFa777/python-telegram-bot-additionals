#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='telegram_addons',
      version='0.1.0',
      description='Extensions for python-telegram-bots',
      author='Kryglyash',
      packages=find_packages(),
      )
